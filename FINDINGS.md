# Findings & Decisions Log

Auto-generated by ralph. Each task logs its decisions and findings here.

---

## Task 1.1: Initialize Go module and directory structure

- Created `go.mod` manually (text file) since Go is not installed on this machine. Module path: `github.com/syndg/deck`, minimum Go 1.22.
- Created all 11 directories from the PRD spec: `cmd/daemon`, `cmd/deck`, `internal/daemon`, `internal/sandbox`, `internal/runtime`, `internal/services/events`, `internal/db`, `internal/config`, `internal/domain`, `internal/client`, `configs/defaults`.
- Added `.gitkeep` to each directory so they're tracked by git (all are currently empty).
- Created `.gitignore` with all specified patterns: Go binaries, vendor, `.env`, SQLite files, `deck`/`deck-daemon` binaries, `/bin/`, `/dist/`.
- **Important for future tasks:** Go is not installed on this machine. Task 1.2 (`go get`) and 1.3 (`go build`) will need Go installed first. Consider `brew install go` or similar.

---

## Task 1.2: Add core dependencies

- Installed Go 1.26.0 via `brew install go` (was not previously on this machine).
- Added all four core dependencies via `go get`: `modernc.org/sqlite` v1.46.1, `github.com/spf13/cobra` v1.10.2, `gopkg.in/yaml.v3` v3.0.1, `github.com/google/uuid` v1.6.0.
- The `go` directive in `go.mod` was auto-bumped from `1.22` to `1.24.0` by `go get` (minimum required by the dependency graph). This is fine — still satisfies the "minimum Go 1.22" requirement from the PRD.
- All deps are marked `// indirect` since no source files import them yet. They'll become direct once task 1.3+ adds Go files with imports.
- Ran `go mod tidy` as specified but it stripped all deps (no source files to reference them). Re-ran `go get` after to restore them. Future `go mod tidy` runs after source files exist will correctly classify direct vs indirect.
- `go.sum` generated with 35 lines of checksums for all dependencies and their transitive deps.

---

## Task 1.3: Create entry point stubs

- Created `cmd/daemon/main.go` — minimal main package that prints "deck daemon starting...".
- Created `cmd/deck/main.go` — minimal main package that prints "deck cli".
- Both files match the PRD spec exactly.
- Verified both compile successfully with `go build ./cmd/daemon` and `go build ./cmd/deck`.
- Verified both binaries produce the expected output when run.
- The `.gitkeep` files in `cmd/daemon/` and `cmd/deck/` are now redundant (real Go files exist) but left in place — harmless and not part of this task's scope.

---

## Task 2.1: Create core domain types

- Created `internal/domain/types.go` with all domain types exactly as specified in the PRD.
- Types: `Objective`, `Plan`, `Stream`, `AgentSession`, `MailMessage`, `Event` — all with JSON struct tags.
- String-typed enums: `ObjectiveStatus` (6 values), `PlanStatus` (6 values), `AgentRole` (4 values), `EventType` (12 values).
- `Stream.Status` and `AgentSession.Status` are plain `string` (not typed enums) per PRD spec — future tasks may want to add typed status enums for these.
- Only stdlib `time` package imported. No external dependencies needed.
- Verified file compiles cleanly with `go build ./internal/domain/`.

---

## Task 2.2: Create sandbox provider interface

- Created `internal/sandbox/provider.go` with `SandboxProvider` and `Sandbox` interfaces exactly as specified in the PRD.
- Created `internal/sandbox/types.go` with all supporting types: `SandboxStatus` (4 constants: Running, Stopped, Creating, Error), `CreateOpts`, `ResourceSpec`, `ExecOpts`, `ExecResult`, `VolumeMount`.
- Used `time.Duration` for `AutoStop`, `AutoDelete` (in `CreateOpts`) and `Timeout` (in `ExecOpts`) — more idiomatic Go than raw int minutes/seconds.
- All struct fields have JSON tags for future API serialization.
- These are interfaces only — no implementations. Concrete providers (e.g., Daytona) will be added in a later phase.
- Verified package compiles cleanly with `go build ./internal/sandbox/`.

---

## Task 2.3: Create agent runtime interface

- Created `internal/runtime/runtime.go` with `AgentRuntime` and `AgentProcess` interfaces plus supporting types (`AgentOpts`, `AgentMessage`, `AgentEvent`, `AgentResult`) exactly as specified in the PRD.
- Imports the sandbox package (`github.com/syndg/deck/internal/sandbox`) for the `sandbox.Sandbox` parameter in `Spawn`. Named the parameter `sb` to avoid shadowing the package name.
- Added JSON struct tags to all types for future API serialization consistency (matching the pattern from domain types and sandbox types).
- These are interfaces only — no implementations. Concrete runtimes (e.g., Claude Code CLI adapter) will be added in a later phase.
- Verified package compiles cleanly with `go build ./internal/runtime/`.

---

## Task 2.4: Create configuration types and YAML loader

- Created `internal/config/config.go` with all config struct types exactly as specified in the PRD: `Config`, `DaemonConfig`, `SandboxConfig`, `ResourceConfig`, `AgentsConfig`, `PlanningConfig`, `WatchdogConfig`, `ToolsConfig`.
- Implemented `Load(path string) (*Config, error)` — reads YAML via `os.ReadFile` + `yaml.Unmarshal`. Returns `Default()` when file not found (using `errors.Is(err, os.ErrNotExist)`). Unmarshals onto a `Default()` base so missing YAML keys retain defaults.
- Implemented `Default() *Config` with sensible defaults: listen `0.0.0.0:9800`, data_dir `~/.deck/data`, provider `daytona`, max_concurrent `8`, max_depth `2`, model `claude-sonnet-4-20250514`, quality_gates `[lint, test, build]`.
- Implemented `ExpandPaths()` which expands `~` prefix in `DataDir` to the user's home directory via `os.UserHomeDir()`. Also added a helper `expandTilde` used by both `ExpandPaths` and `Load` (to handle `~` in the config file path itself).
- `gopkg.in/yaml.v3` is now a direct dependency after `go mod tidy` (was indirect before since no source files imported it).
- Verified the entire project compiles cleanly with `go build ./...`.

---

## Task 3.1: Create database manager

- Created `internal/db/db.go` with `DB` struct wrapping `*sql.DB`, plus `Open`, `Close`, `Conn`, and `Migrate` methods per PRD spec.
- `Open` creates `dataDir` via `os.MkdirAll`, opens SQLite at `{dataDir}/deck.db` with driver `"sqlite"` (modernc), and sets all three pragmas (`journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000`) via `ExecContext`.
- Set `SetMaxOpenConns(1)` since SQLite has limited write concurrency — ensures pragmas apply to the single reused connection.
- If any pragma fails, the connection is closed before returning the error (prevents resource leak).
- Created a minimal `internal/db/migrations.go` stub with `RunMigrations(db *sql.DB) error` returning nil — needed so `Migrate()` compiles. Task 3.2 will replace this with the full schema migration SQL.
- Re-added `modernc.org/sqlite` v1.46.1 via `go get` (had been stripped by `go mod tidy` since nothing imported it). Ran `go mod tidy` after; `modernc.org/sqlite` is now a direct dependency.
- Verified full project compiles with `go build ./...`.

---

## Task 3.2: Create database schema migrations

- Replaced the stub `RunMigrations` in `internal/db/migrations.go` with the full implementation containing all schema DDL.
- Migration SQL defines 7 tables: `objectives`, `plans`, `streams`, `agent_sessions`, `mail`, `events`, `merge_queue` — all using `CREATE TABLE IF NOT EXISTS` for idempotency.
- 3 indexes created: `idx_mail_to_unread` (composite on `to_agent, read, created_at`), `idx_events_type`, `idx_events_objective`.
- Timestamps stored as `INTEGER` (Unix seconds) per PRD convention — domain types use `time.Time` but stores will convert.
- JSON arrays (`quality_gates`, `file_scope`, `dependencies`) stored as `TEXT` with default `'[]'`.
- `RunMigrations` uses `db.ExecContext` with `context.Background()` to execute the entire migration SQL string.
- `DB.Migrate()` in `db.go` was already wired to call `RunMigrations` from task 3.1 — no changes needed there.
- Verified full project compiles with `go build ./...`.

---

## Task 3.3: Create objectives and agents stores

- Created `internal/db/objectives.go` with `ObjectiveStore` — `Create`, `Get`, `List`, `UpdateStatus` methods per PRD spec.
- Created `internal/db/agents.go` with `AgentStore` — `Create`, `Get`, `ListByObjective`, `UpdateStatus` methods per PRD spec.
- Both stores use `*sql.DB` directly (no ORM), generate UUIDs via `github.com/google/uuid` when ID is empty, and store/read timestamps as Unix seconds (converting to/from `time.Time`).
- `ObjectiveStore.Create` defaults status to `"planning"` if empty; `AgentStore.Create` defaults status to `"pending"` if empty (matching DB schema defaults).
- `UpdateStatus` methods check `RowsAffected()` and return wrapped `sql.ErrNoRows` if no row matched — provides clear feedback when updating a non-existent ID.
- `Get` methods wrap `sql.ErrNoRows` with context (e.g., "getting objective {id}: sql: no rows...") so callers can use `errors.Is(err, sql.ErrNoRows)` for 404 handling.
- `github.com/google/uuid` became a direct dependency after `go mod tidy` (was indirect before).
- Verified full project compiles with `go build ./...`.

---

## Task 3.4: Create mail and events stores

- Created `internal/db/mail.go` with `MailStore` — `Send`, `GetUnread`, `MarkRead` methods per PRD spec.
- Created `internal/db/events.go` with `EventStore` — `Insert`, `ListByObjective`, `ListRecent` methods per PRD spec.
- Both stores follow the same patterns established in task 3.3: `*sql.DB` directly, timestamps as Unix seconds, error wrapping with context.
- `MailStore.Send` sets `msg.ID` from `LastInsertId()` after insert (AUTOINCREMENT column); same pattern in `EventStore.Insert` for `event.ID`.
- `MailMessage.Read` (bool) maps to SQLite `read INTEGER` — scanned as `int64` and converted (`readInt != 0`).
- `EventStore` extracts a shared `scanEvents` helper since `ListByObjective` and `ListRecent` have identical scan logic.
- `MarkRead` checks `RowsAffected` and returns wrapped `sql.ErrNoRows` if no row matched (consistent with `UpdateStatus` pattern from task 3.3).
- Verified full project compiles with `go build ./...`.

---

## Task 3.5: Create event bus with persistence

- Created `internal/services/events/bus.go` with `Bus`, `Subscriber` type, `NewBus`, `Publish`, and `Subscribe` — all per PRD spec.
- Created `internal/services/events/persistent.go` with `PersistentBus` wrapping `Bus` and `db.EventStore`.
- `Bus.Publish` uses `sync.RWMutex` (RLock for publish, full Lock for subscribe/unsubscribe) and non-blocking sends via `select` with `default` to drop events for slow subscribers.
- `Subscribe` returns a buffered channel and an unsubscribe closure that deletes from the map and closes the channel. Subscriber IDs are monotonically incrementing ints.
- `PersistentBus.Publish` persists via `store.Insert(context.Background(), &event)` first, then delegates to `Bus.Publish` for broadcasting. DB errors are logged but don't block event delivery.
- Logging uses `log/slog` throughout — info for published events, warn for dropped events, error for persistence failures.
- Verified full project compiles with `go build ./...`.

---

## Task 4.1: Create daemon server

- Created `internal/daemon/daemon.go` with `Daemon` struct, `New`, `Start`, and `Shutdown` methods per PRD spec.
- `New` expands config paths, opens DB, runs migrations, creates all 4 stores (`ObjectiveStore`, `AgentStore`, `MailStore`, `EventStore`), creates `PersistentBus`, sets up `http.ServeMux` and `http.Server`, and calls `registerRoutes()`.
- If migrations fail, the DB connection is closed before returning the error (prevents resource leak, same pattern from task 3.1).
- `Start` returns `nil` on `http.ErrServerClosed` (expected during graceful shutdown), propagates all other errors.
- `Shutdown` closes the HTTP server first, then the database — ordered to prevent requests hitting a closed DB.
- `registerRoutes()` is a no-op stub — tasks 4.2–4.4 will populate it with SSE, objectives, health, and status endpoints.
- Logger uses `slog.Default().With("component", "daemon")` for structured logging with component context.
- Verified full project compiles with `go build ./...`.

---

## Task 4.2: Create SSE event stream endpoint

- Created `internal/daemon/sse.go` with `handleSSE` method per PRD spec.
- Sets required SSE headers (`Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`), checks for `http.Flusher` support, subscribes to the event bus with buffer size 64.
- Events are JSON-marshaled and written in SSE `data: {json}\n\n` format with `Flush()` after each event.
- Uses `select` on `r.Context().Done()` (client disconnect) and the subscriber channel for the event loop.
- Registered as `GET /events` in `registerRoutes()` — replaced the no-op stub. Future tasks (4.3, 4.4) will add more routes to this function.
- Logs SSE client connect/disconnect with remote address for observability.
- Verified full project compiles with `go build ./...`.

---

## Task 4.3: Create objectives REST endpoints

- Created `internal/daemon/routes.go` with `registerRoutes`, three objective handlers (`handleCreateObjective`, `handleGetObjective`, `handleListObjectives`), and two helpers (`writeJSON`, `writeError`).
- Moved `registerRoutes` from `daemon.go` to `routes.go` — daemon.go no longer has route registration code.
- `handleCreateObjective` validates JSON body, requires non-empty `description`, creates objective via store, publishes `EventObjectiveCreated` event, responds 201.
- `handleGetObjective` uses Go 1.22 `r.PathValue("id")` for path parameter extraction; returns 404 via `errors.Is(err, sql.ErrNoRows)` check.
- `handleListObjectives` returns empty JSON array `[]` instead of `null` when no objectives exist (better API ergonomics).
- Health and status route registrations deferred to task 4.4 — only registered routes with existing handler implementations to keep code compilable.
- Verified full project compiles with `go build ./...`.

---

## Task 4.4: Create health and status endpoints

- Added `handleHealth` and `handleStatus` handlers to `internal/daemon/routes.go` per PRD spec.
- `handleHealth` returns `{"status": "ok"}` with 200 — minimal, no DB queries.
- `handleStatus` queries all objectives via `objectives.List`, counts them by status into a `map[string]int`, and computes uptime from `d.startTime` using `time.Since().Round(time.Second).String()`.
- Defined `StatusResponse` struct in `routes.go` with JSON tags matching the PRD spec: `status`, `uptime`, `objectives`.
- Registered `GET /health` and `GET /status` routes in `registerRoutes` — completes the route registrations that were deferred from task 4.3.
- Verified full project compiles with `go build ./...`.

---

## Task 4.5: Wire daemon entry point

- Rewrote `cmd/daemon/main.go` from the stub to a full daemon entry point per PRD spec.
- Parses `--config` flag (default `~/.config/deck/config.yaml`) via `flag` package, loads config via `config.Load`, and calls `cfg.ExpandPaths()`.
- Creates daemon via `daemon.New(cfg)`, starts it in a goroutine, and waits for `os.Interrupt` or `syscall.SIGTERM`.
- On signal, performs graceful shutdown with a 10-second timeout context via `daemon.Shutdown`.
- All logging uses `log/slog` — errors exit with `os.Exit(1)`, startup/shutdown messages logged at info level.
- Verified full project compiles with `go build ./...`.
